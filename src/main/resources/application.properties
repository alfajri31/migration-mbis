server.port=9123
#spring.datasource.url=jdbc:postgresql://localhost:5432/mbis_dev
#spring.datasource.username=postgres
#spring.datasource.password=postgres

spring.datasource.url=jdbc:postgresql://10.100.20.53:5432/mbis_dev
spring.datasource.username=postgres
spring.datasource.password=askrindo

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE


agent.host.url = http://fajris-mac-mini.tail2fab46.ts.net:11434/api/chat

ai.model.picture=qwen3-vl:4b
#prompt.user.picture=Extract ONLY explicit field-value relevant pairs which is still empty. And Return ONLY WITH JSON.
#system.feedback.picture=You extract structured data from image. Only return valid JSON array. No explanation.
prompt.user.picture=Given an image and backend field data, extract ONLY values for fields that are empty (null, "", or missing). Only include values explicitly visible in the image. Ignore already filled fields. Return ONLY a valid JSON array where each item contains "field" and "value".
system.feedback.picture=You extract structured data from images. Only fill fields that are empty. Do not guess or infer missing values. Ignore unclear text. Output must be a valid JSON array only, with no explanation or extra text.

ai.model.base.knowledge=llama3.2:3b
ai.model.context.base.knowledge=128000
ai.data.crawl.limit=20