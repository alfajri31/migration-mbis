server.port=9123
spring.datasource.url=jdbc:postgresql://localhost:5432/mbis_dev
spring.datasource.username=postgres
spring.datasource.password=postgres

#spring.datasource.url=jdbc:postgresql://10.100.20.53:5432/mbis_dev
#spring.datasource.username=postgres
#spring.datasource.password=askrindo

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

ai.model=llama3.2
ai.model.picture=qwen3-vl:4b
#prompt.user=Extract ONLY explicit field-value pairs which is still empty.\n\nRules:\n1. Use ONLY the OCR text as the source of truth\n2. Field And Value Only In OCR Text\n3. Return ONLY JSON.
prompt.user=Extract ONLY explicit field-value relevant pairs which is still empty. And Return ONLY WITH JSON.
#system.feedback=You extract structured data from noisy OCR text. Only return valid JSON array. No explanation.
system.feedback=You extract structured data from image. Only return valid JSON array. No explanation.